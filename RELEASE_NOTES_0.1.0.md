# TraceDigest v0.1.0

TraceDigest 是基于 TraceMemo 的非商业二次开发版本，首版聚焦只读微信聊天总结 Agent。

## 主要变化

- Agent 不再依赖预先划分的意图类别，由模型根据用户要求选择只读工具。
- 支持按群聊最近若干条消息、指定时间范围和指定群成员进行总结。
- 群名和成员名先搜索匹配，存在歧义时要求用户确认。
- Agent 工具仅提供读取权限，不支持删除、修改或主动发送微信消息。
- 支持自定义 OpenAI Compatible 模型供应商和自定义总结指令。
- 移除左侧“API”和“问问微信”入口。
- 应用、用户数据和机器人凭据使用独立的 TraceDigest 身份，不覆盖 TraceMemo。
- 自动更新关闭，用户通过 GitHub Releases 手动下载新版。

## 注意事项

- 仅支持用户有权访问的本地微信数据。
- 安装包暂未签名，Windows 可能显示 SmartScreen 提示。
- 首次安装需要重新配置 AI 供应商并重新扫码登录机器人。
- 本项目不是微信官方项目，且受上游 TraceMemo 的非商业使用限制约束。
